# Copy this file to .env and fill in your key. Optional -- if unset (or
# invalid), the /explain endpoint falls back to a deterministic
# template-based explanation instead of a Gemini-generated one; nothing
# else in the app requires it.
GEMINI_API_KEY=
